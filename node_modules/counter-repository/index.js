module.exports = {
  getCounter(counterName) {
    throw new Error('You must configure a postges connection before querying')
  },

  saveCounter(counter) {
    throw new Error('You must configure a postges connection before saving')
  }

}